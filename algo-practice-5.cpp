#include <iostream>

using namespace std;

int main()
{
    int a, b;
    cout << "Input first number:";
    cin >> a;
    cout << "Input second number:";
    cin >> b;
    if (a > b)
        cout << "";
    else if (a < b)
        cout << "";
    else
        cout << "";
    return 0;
}